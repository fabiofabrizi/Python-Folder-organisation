# Python-Folder-organisation


This is a small repository containing some scripts to manage folders.
How it works:
- Specify a folder where the python script should work
- The scripts will manage the folder according to extensions.
- Can use regex to match the terms in the title